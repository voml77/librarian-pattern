[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "librarian-pattern-demo"
version = "0.1.0"
description = "Minimal reference implementation of the Librarian Pattern."
requires-python = ">=3.11"

[tool.setuptools]
packages = ["librarian_pattern"]